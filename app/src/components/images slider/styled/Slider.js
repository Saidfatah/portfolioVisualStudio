import { Box } from "../Box";
import styled from "styled-components";

const Slider = styled(Box)``;

Slider.defaultProps = {
  border: 1,
  borderWidth: 2,
  bg: "brand100",
  p: 3
};

export default Slider
